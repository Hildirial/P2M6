name: Assign to project

on:
  issues:
    types: [labeled]

jobs:
  assign:
    runs-on: ubuntu-latest
    name: Assign to project
    steps:
    - name: Assign to project
      uses: ./.github/actions/assign-to-project
      with:
        MY_GITHUB_TOKEN: ${{ secrets.MY_GITHUB_TOKEN }}
